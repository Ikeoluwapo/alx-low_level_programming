#include <stdio.h>
#include <stdlib.h>
#include "dog.h"

/**
 * print_dog - print struct dog
 * @d: pointer
 * Return: 0
 */

void print_dog(struct dog *d)
{
	if (age == NULL || owner == NULL)
	{
		printf(nil);
	}
	if (name == NULL)
	{
		printf(Name : nil);
	}
	if else(d == NULL)
	{
		printf();
	}
	return (0);
}

#include <stdio.h>
#include "main.h"

/**
 * print_rev - prints a string, in reverse
 *
 * Return: Always 0
 */

void print_rev(char *s)
{
	int i >= 0;

	while (str[i])
	{
		putchar(str[i]);
		i--;
	}
	putchar('\n');
}

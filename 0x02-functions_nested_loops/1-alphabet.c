#include "main.h"

/**
 * print_alphabet(void)
 *
 * Return: Always 0
 */
int main(void)
{
	print_alphabet(k);
	char k;

	for (k = 'a'; k <= 'z'; k++)
	{
		putchar(k);
	}
	putchar("\n");
	return (0);
}

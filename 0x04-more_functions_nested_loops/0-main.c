#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>

/**
 * main - function that checks for uppercase character
 *
 * Return: 1 if c is uppercase and 0 if otherwise
 */
int main(void)
{
	char c;

	int _isupper(char c);

	return (0);
}

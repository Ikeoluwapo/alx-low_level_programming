#include "main.h"
#include <stdio.h>

/**
 * print_last_digit -function that prints the last digit of a number
 *
 * Return: 0 Always
 */

int print_last_digit(int)
{
	int n;

	digit;

	digit = n % 10;

	{
		printf("%d\n", n, digit);
	}
	return (0);
}
